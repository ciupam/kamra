export const getHeight = () => window.innerHeight;
export const getWidth = () => window.innerWidth;
export const getWindowSize = () => [getWidth(), getHeight()];
export const sin = (x) => Math.sin(x);
export const cos = (x) => Math.cos(x);
